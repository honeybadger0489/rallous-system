Downloaded files from the official Modrinth CDN land here after:

```bash
./scripts/download-pack.sh --starter
./scripts/download-pack.sh
```

`.jar` / pack `.zip` binaries are gitignored on purpose. The canonical list is `../pack/mods.json` and `../pack/modrinth.index.json`.
